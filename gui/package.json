{
    "name": "argus-gui",
    "version": "0.1.0",
    "description": "Desktop GUI for Argus Visual Regression Testing",
    "type": "module",
    "scripts": {
        "start": "bun run build:dev && electrobun dev",
        "build:dev": "bun install && electrobun build",
        "build:prod": "bun install && electrobun build --production"
    },
    "dependencies": {
        "electrobun": "^0.4.0"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "typescript": "^5.7.2"
    }
}